import React from 'react';
import { useTheme } from '@material-ui/styles';

//-----------------------|| LOGO SVG ||-----------------------//

const Logo = () => {
    const theme = useTheme();

    return (
      <h1>Brave Ag</h1>
    );
};

export default Logo;
